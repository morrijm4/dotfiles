CC = g++
CMD = cmd
SRC = main.cpp
DEBUG_FLAGS = -g -fsanitize=address -fno-omit-frame-pointer 
CFLAGS = -Wall -Wextra -o $(CMD) -std=c++17 $(DEBUG_FLAGS)

$(CMD): $(SRC)
	$(CC) $(CFLAGS) $(SRC)

run: $(CMD)
	./$(CMD)

clean:
	rm $(CMD)

install: $(CMD)
	cp $(CMD) /usr/local/bin

flags:
	echo "$(CFLAGS)" | tr ' ' '\n' > compile_flags.txt
